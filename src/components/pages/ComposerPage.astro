---
import QuantumCircuitBuilder from "../QuantumCircuitBuilder.astro";
import type { Lang } from "../../config/i18n";
import type { ComposerMessages } from "../../locales/composer";

interface Props {
  lang: Lang;
  messages: ComposerMessages;
}

const { lang, messages } = Astro.props as Props;
---

<section class="relative overflow-hidden bg-hero-gradient">
  <div class="mq-container py-16">
    <p class="mq-eyebrow text-muted-foreground mb-4">{messages.eyebrow}</p>
    <h1 class="text-4xl md:text-5xl font-bold text-foreground mb-4 tracking-tight">
      {messages.title}
    </h1>
    <p class="text-base md:text-lg text-muted-foreground max-w-2xl leading-relaxed">
      {messages.subtitle}
    </p>
    <p class="mt-4 text-sm text-muted-foreground/85 max-w-2xl leading-relaxed">
      {messages.roadmapNote}
    </p>
  </div>
</section>

<section class="mq-container my-10">
  <div class="mq-card bg-background p-4 sm:p-6">
    <QuantumCircuitBuilder lang={lang} heading="" />
  </div>
</section>
